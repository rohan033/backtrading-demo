import { PlaceholderPage } from '../../layout/page-meta'

export default function HistoryPage() {
  return (
    <PlaceholderPage
      title="History & Reports"
      description="Exportable session history and report snapshots. Historical events from live strategies remain available under Trade → Activity."
    />
  )
}
